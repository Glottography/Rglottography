% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_datasets.R
\name{install_datasets}
\alias{install_datasets}
\title{Install Glottography datasets}
\usage{
install_datasets(datasets, update = c("missing", "outdated", "always"))
}
\arguments{
\item{datasets}{Character vector specifying the datasets to install, or one
of the following special values:
\itemize{
\item \code{"all"}: install all available datasets.
\item \code{"outdated"}: install all datasets for which an update is
available (new version or updated timestamp).
\item \code{"missing"}: install all datasets that are not yet installed.
}}

\item{update}{Character string indicating when selected datasets should be
installed. One of:
\itemize{
\item \code{"outdated"}: install only if an update is available on Zenodo.
\item \code{"missing"}: install only if the dataset is not present locally.
\item \code{"always"}: install regardless of local status.
}

This argument is ignored when \code{datasets} is \code{"all"}, \code{"outdated"},
or \code{"missing"}.}
}
\value{
Invisibly returns the installed datasets,
or \code{NULL} if none were installed.
}
\description{
Installs Glottography datasets by checking their status in the registry and
downloading them from the Zenodo Glottography community.
}
\details{
By default, datasets are cached in the directory returned
by \code{tools::R_user_dir("Rglottography", "data")}.
You can change the cache location using \code{set_cache_dir(path)}.
}
